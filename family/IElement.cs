namespace nilnul.set.family
{
	/// <summary>
	/// the member of the member of the family; the obj, which might be not a set;
	/// </summary>
	/// vs:
	///		<see cref="IMember"/>, like 1 is a member of {1,2,3}
	///		whileas this means that the element is a monolithic|atomic in the discourse and cannot be regarded as a collection.
	public interface IElement { }
}
